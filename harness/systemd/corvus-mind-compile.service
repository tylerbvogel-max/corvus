[Unit]
Description=Corvus Mind skill compiler (lesson clusters -> ~/.claude/skills)

OnFailure=corvus-job-alert@%n.service

[Service]
Type=oneshot
ExecStart=/usr/bin/curl -s --fail-with-body --max-time 900 -X POST http://localhost:8005/compile/run
